import React from "react";
import { OStyled } from "../../GlobalStyles";

/**
 * the O marker
 * @param props
 * @returns {*}
 * @constructor
 */
const Zero = (props) => {
  return <OStyled>O</OStyled>;
};

export default Zero;
